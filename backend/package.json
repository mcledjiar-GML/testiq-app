{
  "name": "iq-test-backend",
  "version": "1.0.0",
  "description": "Backend pour l'application de test de QI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:quality": "node scripts/test-v2-system.js full",
    "test:quick": "node scripts/test-v2-system.js quick",
    "test:audit": "node scripts/test-v2-system.js audit",
    "quality:gate": "node -e \"require('./middleware/quality-gate').validateRelease().then(r=>process.exit(r.ready_for_release?0:1))\"",
    "quality:report": "node scripts/test-v2-system.js full && node scripts/test-v2-system.js audit",
    "ci:test": "npm run test:quality && npm run test:audit",
    "precommit": "npm run test:quick",
    "seed:demo": "node scripts/seed-demo.js --env=.env.demo",
    "api:demo": "dotenv -e .env.demo -- npm --prefix api run dev",
    "web:demo": "dotenv -e .env.demo -- npm --prefix web run dev -- --port 5173",
    "demo:up": "node -e \"console.log('\\n=== OUVRE 2 TERMINAUX SEPARES POUR LES TUNNELS CLOUDFLARED ===\\n> cloudflared tunnel --url http://localhost:5173\\n> cloudflared tunnel --url http://localhost:4000\\n')\" && concurrently -n API,WEB -c auto \"npm run api:demo\" \"npm run web:demo\"",
    "demo:down": "kill-port 4000 5173 && node -e \"console.log('Tunnels cloudflared : fermer les 2 terminaux manuellement.')\"",
    "check:demo": "node scripts/check-demo.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "openai": "^4.8.0",
    "ulid": "^3.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
